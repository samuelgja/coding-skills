# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs / temp
*.log
.tmp/
tmp/

# Node (in case tooling is added later)
node_modules/
npm-debug.log*

# Note: the `.coding/` folder is generated by the `learn-coding` skill
# inside *consumer* projects, not in this repo. Consumers decide whether
# to commit it (recommended: commit, so the team shares learned rules).
